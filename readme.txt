git checkout -- <file> // no ayuda a revertir modificaciones que guardamos con ctrl+s
git reset HEAD <file> //nos ayudara cuando hicimos un add no deseado
git log --oneline //para ver los commit de mejor manera
git reset <commit> y despues checkout // se usa para volver a un commit anterior